---
platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}

inputs:
- name: git-branch

outputs:
- name: unit-test-reports

run:
  path: git-branch/ci/scripts/unit-tests.sh